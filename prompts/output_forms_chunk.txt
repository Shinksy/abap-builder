Output chunk prompt:
Generate only FORM...ENDFORM routines that prepare and present generated results.

Rules:
- Use the exact output FORM names and output fields from the supplied chunk contract.
- Generate ALV logic only when an ALV FORM name is present in the chunk contract.
- Generate CSV logic only when a CSV FORM name is present in the chunk contract.
- Use classical SAP ECC-compatible statements.
- Do not add USING, CHANGING or TABLES parameters.
- Do not generate SAP database reads.
- Do not generate business-processing, event-block, or declaration logic.
- Return only ABAP source for these routines.

When ALV output is required:

- Use classical SAP ECC ALV.
- Use REUSE_ALV_GRID_DISPLAY.
- Do not generate field-catalogue population code.
- Do not REFRESH t_fieldcat, CLEAR w_fieldcat, assign w_fieldcat-* fields, or APPEND w_fieldcat TO t_fieldcat.
- The application inserts the field-catalogue entries deterministically from the approved output contract.
- Pass the field catalogue using IT_FIELDCAT.

When file output is required:
- Use the existing global filename variable from the supplied chunk contract.
- Treat the filename variable as the dataset path only.
- Do not use the filename variable as a CSV header, row, or concatenation buffer.
- Use the exact CSV line variable from the supplied chunk contract for generated row text.
- Build each CSV row in the CSV line variable, then TRANSFER the CSV line variable TO the filename variable.
- Do not create local or undeclared filename variables.

Output requirements:
{{OUTPUT_REQUIREMENTS}}

Relevant SAP DDIC metadata:
{{DDIC_METADATA}}

Output chunk contract:
{{CHUNK_CONTRACT}}
