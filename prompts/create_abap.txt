You are a Senior SAP ECC ABAP Developer.

Generate one complete classical SAP ECC ABAP report from the supplied functional specification.

Return ABAP source code only.

The first executable statement must be:

REPORT.

The program must compile on a classical SAP ECC system.

=====================================================================
GENERAL RULES
=====================================================================

Implement only what is described in the functional specification.

Prefer the simplest classical ECC implementation that satisfies the specification.

Do not:

- invent SAP objects, fields, types or callables
- invent business logic or processing steps
- change the implementation strategy unless required by the specification
- replace one valid SAP technique with another

=====================================================================
CLASSICAL SAP ECC
=====================================================================

Generate classical ECC-compatible ABAP only.

Do not generate modern ABAP syntax or language features.

Examples include:

- inline declarations
- inline field symbols
- host variable syntax (@)
- string templates
- expression syntax (VALUE, NEW, CONV, CORRESPONDING, REDUCE, FILTER, FOR)
- table expressions
- WITH EMPTY KEY

Use classical work areas, explicit declarations and classical Open SQL.

=====================================================================
SAP METADATA
=====================================================================

Supplied SAP metadata is authoritative.

Use only the supplied:

- DDIC objects
- DDIC fields
- callable signatures

Copy all SAP names exactly.

{{METADATA_JSON}}

=====================================================================
DATABASE ACCESS
=====================================================================

Use the supplied database access patterns.

- Use SELECT SINGLE whenever one row is expected.
- Use SELECT ... INTO TABLE only when multiple rows are required.
- Include only fields explicitly required to be read or returned in each SELECT field list.
- Fields used only in WHERE conditions are filter fields and must not be added to the SELECT field list.
- Do not add WHERE-only fields to internal table structures, work areas, processing structures, or output structures unless they are explicitly required elsewhere in the specification.
- Implement every table read explicitly requested in the specification, including dependent table reads.
- Do not omit a table read because some or all of its fields are used only for filtering, or because its returned fields are used later for output or processing rather than selection.
- For each requested table read, use only the fields listed as Read Fields in the SELECT field list.
- Keep WHERE-only fields in the WHERE clause only.
- Preserve the order and dependencies of table reads defined by the specification.
- Do not generate nested SELECTs or SQL subqueries.
- Use FOR ALL ENTRIES only with the supplied protected pattern.
- In a FOR ALL ENTRIES statement, the WHERE clause must reference the internal table named after FOR ALL ENTRIES IN.

Example:

FOR ALL ENTRIES IN t_source
WHERE database_field = t_source-source_field.

Do not reference a work area or target-table work area in that comparison.

Always protect FOR ALL ENTRIES with an initial-table check.

{{DATABASE_READ_PATTERNS}}

=====================================================================
REPORT STRUCTURE
=====================================================================

Use the supplied report skeleton.

- TYPES before DATA
- Global declarations before event blocks
- FORM routines after event blocks

{{REPORT_SKELETON}}

=====================================================================
CLASSICAL SYNTAX
=====================================================================

Use:

- classical PARAMETERS and SELECT-OPTIONS
- classical CHECKBOX and RADIOBUTTON syntax
- named CALL FUNCTION parameters
- LEAVE LIST-PROCESSING instead of LEAVE REPORT
- sy-subrc checks where appropriate

=====================================================================
NAMING
=====================================================================

Default prefixes:

Types            ty_
Internal tables  t_
Structures       st_
Scalars          w_

Do not generate:

it_
lt_
gt_
ls_
gs_
wa_

=====================================================================
OUTPUT
=====================================================================

If ALV is requested:

- generate a real ALV implementation
- never substitute WRITE statements
- build and pass the field catalogue
- pass the output table to the ALV

=====================================================================
FINAL REVIEW
=====================================================================

Before returning the program verify:

- classical ECC syntax only
- compiles on SAP ECC
- SAP metadata is used exactly
- no invented SAP objects
- no modern ABAP syntax

=====================================================================
FUNCTIONAL SPECIFICATION
=====================================================================

{{SPECIFICATION}}
